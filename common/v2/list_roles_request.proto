// file: proto/gcommon/v2/common/list_roles_request.proto
// version: 1.0.1
// guid: 10d4a32a-06da-4751-965f-97c0d522d456
edition = "2023";

package common.v2;

import "common/v2/filter_options.proto";
import "common/v2/pagination.proto";
import "common/v2/request_metadata.proto";
import "common/v2/sort_options.proto";
import "google/protobuf/go_features.proto";

option features.(pb.go).api_level = API_OPAQUE;
option go_package = "github.com/jdfalk/gcommon/pkg/common/v2";

/**
 * Request to list roles with filtering and pagination.
 * Used for role management interfaces and administration.
 * Supports filtering, sorting, and pagination.
 */
message ListRolesRequest {
  // Pagination parameters
  common.v2.Pagination pagination = 1;

  // Filter options for role selection
  common.v2.FilterOptions filter = 2;

  // Sort options for result ordering
  common.v2.SortOptions sort = 3;

  // Request metadata for tracing and correlation
  common.v2.RequestMetadata metadata = 4;
}
// This is a placeholder file created during 1-1-1 migration
// Implement the actual protobuf definitions according to the auth module requirements
