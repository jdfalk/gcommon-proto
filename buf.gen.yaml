version: v2
clean: true # we should be regenerating all these files with teh script copilot so this is a TODO if we are not.
managed:
  enabled: true
  disable:
    # Don't modify any file option or field option for googleapis
    - module: buf.build/googleapis/googleapis
  override:
    # Override all generated files to use the same Go module path
    - file_option: go_package
      value: github.com/jdfalk/gcommon/sdk/go
plugins:
  # Go: use local protoc-gen-go with managed mode
  - local: protoc-gen-go
    out: sdks/go
    strategy: all
    opt:
      - paths=source_relative
  # Go gRPC services
  - local: protoc-gen-go-grpc
    out: sdks/go
    strategy: all
    opt:
      - paths=source_relative
  # Python messages via protoc built-in generator
  - protoc_builtin: python
    out: sdks/python
    strategy: directory
  # Optional: Python type stubs for better IDE/mypy support (requires protoc >= 3.20)
  - protoc_builtin: pyi
    out: sdks/python
    strategy: directory
  # - protoc_builtin: rust
  #   out: sdks/rust
  #   strategy: directory
# Post-generation commands (Note: buf doesn't support this directly)
# Use: buf generate && python3 scripts/post-buf-generate.py
# Or use the Makefile: make generate
