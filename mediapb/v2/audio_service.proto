// file: proto/gcommon/v2/media/audio_service.proto
// version: 1.0.1
// guid: 0123456-789a-bcde-e7f8-a9b0c1d2e3f4

edition = "2023";

package media.v2;

import "mediapb/v2/analyze_audio_quality_request.proto";
import "mediapb/v2/analyze_audio_quality_response.proto";
import "mediapb/v2/detect_chapters_request.proto";
import "mediapb/v2/detect_chapters_response.proto";
import "mediapb/v2/merge_audio_request.proto";
import "mediapb/v2/merge_audio_response.proto";
import "mediapb/v2/normalize_audio_request.proto";
import "mediapb/v2/normalize_audio_response.proto";
import "mediapb/v2/split_audio_request.proto";
import "mediapb/v2/split_audio_response.proto";
import "google/protobuf/go_features.proto";

option features.(pb.go).api_level = API_OPAQUE;
option go_package = "github.com/jdfalk/gcommon/pkg/mediapb/v2";

/**
 * AudioService provides audio-specific operations including
 * chapter detection, audio processing, and quality analysis.
 */
service AudioService {
  // Detect chapters in audiobook files
  rpc DetectChapters(DetectChaptersRequest) returns (DetectChaptersResponse);

  // Normalize audio levels
  rpc NormalizeAudio(NormalizeAudioRequest) returns (NormalizeAudioResponse);

  // Split audio file into segments
  rpc SplitAudio(SplitAudioRequest) returns (SplitAudioResponse);

  // Merge multiple audio files
  rpc MergeAudio(MergeAudioRequest) returns (MergeAudioResponse);

  // Analyze audio quality metrics
  rpc AnalyzeAudioQuality(AnalyzeAudioQualityRequest) returns (AnalyzeAudioQualityResponse);
}
