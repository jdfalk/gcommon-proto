// file: proto/gcommon/v1/config/v1/synchronization_settings.proto
// version: 1.0.0
// guid: e3f41f73-ecdc-4499-9a1a-8adc64cb1ed1

edition = "2023";

package config.v1;

import "common/v1/conflict_resolution.proto";
import "common/v1/synchronization_frequency.proto";
import "config/v1/synchronization_target.proto";
import "google/protobuf/go_features.proto";
import "buf/validate/validate.proto";


option features.(pb.go).api_level = API_OPAQUE;
option go_package = "github.com/jdfalk/gcommon/pkg/configpb";

message SynchronizationSettings {
  // Whether synchronization is enabled
  bool enabled = 1;

  // Synchronization targets
  repeated config.v1.SynchronizationTarget targets = 2 [(buf.validate.field).repeated.min_items = 1];

  // Synchronization frequency
  common.v1.SynchronizationFrequency frequency = 3;

  // Synchronization conflict resolution
  common.v1.ConflictResolution conflict_resolution = 4;

  // Synchronization metadata
  map<string, string> metadata = 5;
}
