// file: proto/gcommon/v1/config/api/version_promotion_event.proto
// version: 1.0.0
// guid: d231a04f-95ab-4854-a15d-7992ab883362

edition = "2023";

package gcommon.v1.config.api;

import "google/protobuf/go_features.proto";
import "google/protobuf/timestamp.proto";
import "proto/gcommon/v1/common/messages/audit_log.proto";

option features.(pb.go).api_level = API_OPAQUE;
option go_package = "gcommon.v1.config.api";

message VersionPromotionEvent {
  // Source environment
  string source_environment = 1;

  // Target environment
  string target_environment = 2;

  // Promotion timestamp
  google.protobuf.Timestamp timestamp = 3;

  // Promotion user
  string promoted_by = 4;

  // Promotion reason
  string reason = 5;

  // Promotion method
  string method = 6;

  // Promotion success
  bool success = 7;

  // Promotion error
  string error = 8;

  // Promotion metadata
  map<string, string> metadata = 9;
}
