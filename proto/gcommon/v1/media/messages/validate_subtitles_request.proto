// file: proto/gcommon/v1/media/messages/validate_subtitles_request.proto
// version: 1.0.0
// guid: cdef012-3456-789a-a3b4-c5d6e7f8a9b0

edition = "2023";

package gcommon.v1.media;

import "google/protobuf/go_features.proto";

option features.(pb.go).api_level = API_OPAQUE;
option go_package = "v1/media";

// Request to validate subtitle file.
message ValidateSubtitlesRequest {
  string subtitle_file_id = 1;
  ValidationOptions options = 2;
}

// Options for subtitle validation.
message ValidationOptions {
  bool check_timing_overlap = 1; // Check for overlapping subtitle timings
  bool check_encoding = 2; // Validate text encoding
  bool check_format_compliance = 3; // Check format specification compliance
  bool check_reading_speed = 4; // Check if subtitles are readable at given speed
  double max_reading_speed_wpm = 5; // Maximum reading speed in words per minute
}
