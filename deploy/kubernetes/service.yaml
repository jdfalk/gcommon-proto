# file: deploy/kubernetes/service.yaml
# version: 1.0.0
# guid: 8bc1696b-a5c2-45b1-9fc1-9987bf2022d4

apiVersion: v1
kind: Service
metadata:
  name: gcommon-app
  labels:
    app: gcommon-app
spec:
  selector:
    app: gcommon-app
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
