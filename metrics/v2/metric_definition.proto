// file: proto/gcommon/v2/metrics/v2/metric_definition.proto
// version: 1.0.0
// guid: 4368bdba-c3ff-4d80-bfa1-886b807d497b

edition = "2023";

package metrics.v2;

import "common/v2/metric_type.proto";
import "common/v2/metrics_retention_policy_config.proto";
import "metrics/v2/export_config.proto";
import "metrics/v2/label_definition.proto";
import "metrics/v2/metric_type_config.proto";
import "metrics/v2/validation_rules.proto";
import "google/protobuf/go_features.proto";
import "buf/validate/validate.proto";

option features.(pb.go).api_level = API_OPAQUE;
option go_package = "github.com/jdfalk/gcommon/pkg/metricspb/v2";

message MetricDefinition {
  // Unique name for the metric
  string name = 1 [
      (buf.validate.field).string.min_len = 1,
      (buf.validate.field).string.max_len = 100
    ];

  // Type of metric (counter, gauge, histogram, etc.)
  common.v2.MetricsMetricType type = 2;

  // Human-readable description
  string description = 3 [ (buf.validate.field).string.max_len = 1000 ];

  // Unit of measurement (e.g., "bytes", "requests", "seconds")
  string unit = 4;

  // Labels that this metric supports
  repeated LabelDefinition labels = 5;

  // Metric-specific configuration
  MetricTypeConfig config = 6;

  // Retention policy for this metric
  common.v2.MetricsRetentionPolicyConfig retention = 7;

  // Export configuration for this metric
  ExportConfig export_config = 8;

  // Validation rules for metric values
  ValidationRules validation = 9;

  // Tags for metric organization and discovery
  map<string, string> tags = 10;
}
