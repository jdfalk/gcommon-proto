{
  "name": "gcommon",
  "version": "1.0.0",
  "description": "Common Go libraries and Protocol Buffer definitions for distributed systems",
  "private": true,
  "scripts": {
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD --verbose",
    "commitlint-ci": "commitlint --from=origin/main --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3"
  },
  "keywords": [
    "go",
    "protobuf",
    "grpc",
    "distributed-systems",
    "common-libraries"
  ],
  "author": "jdfalk",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
